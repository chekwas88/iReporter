language: node_js
node_js: 
  - "stable"
services:
  - postgresql
before_script:
  - psql -c 'create database ireportertest_db;' -U postgres
cache:
  directories:
    - "node_modules"
after_success: -npm run coverage